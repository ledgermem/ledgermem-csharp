name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet restore test/Mnemo.Tests.csproj
      - run: dotnet build test/Mnemo.Tests.csproj --no-restore -c Release
      - run: dotnet test test/Mnemo.Tests.csproj --no-build -c Release --verbosity normal
